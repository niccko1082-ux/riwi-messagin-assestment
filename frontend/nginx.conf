server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA con react-router (BrowserRouter): cualquier ruta sin archivo estático cae a
    # index.html para que el enrutado lo resuelva el cliente.
    location / {
        try_files $uri /index.html;
    }
}
